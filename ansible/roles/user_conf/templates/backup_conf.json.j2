{
    "backups": [
        {
            "name": "Portainer Volumes",
            "source_folder": "/var/lib/docker/volumes",
            "dest_bucket": "{{ s3_bucket_name }}",
            "excluded": [],
            "retention_days": "30"
        }
    ]
}